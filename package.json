{
  "name": "azkar-monorepo",
  "private": true,
  "workspaces": ["apps/web", "apps/desktop"],
  "scripts": {
    "dev": "npm run dev --workspace=apps/desktop",
    "dev:web": "npm run dev --workspace=apps/web",
    "build": "npm run build --workspace=apps/desktop",
    "build:web": "npm run build --workspace=apps/web",
    "release": "node -e \"const v=process.argv[1]; const {execSync}=require('child_process'); execSync('git tag v'+v,{stdio:'inherit'}); execSync('git push origin v'+v,{stdio:'inherit'});\" --"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
